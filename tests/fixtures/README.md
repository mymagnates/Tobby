# Emulator fixtures

`emulatorPropertyAccess.js` seeds two isolated PM-owned Property Accounts for
Firestore and Storage rule tests. The fixture uses disabled rules only for setup;
each assertion uses an authenticated client context.
